/*
 * UserList Messages
 *
 * This contains all the text for the UserList component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  empty: {
    id: 'app.components.UserList.empty',
    defaultMessage: 'No users',
  },
});
